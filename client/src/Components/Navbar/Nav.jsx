import React from "react";
import { AppBar, Toolbar } from "@mui/material";

const Nav = () => {
  return (
    <>
      <AppBar>
        <Toolbar></Toolbar>
      </AppBar>
    </>
  );
};

export default Nav;
